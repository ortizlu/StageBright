{{!-- {{#if posts}}
<ul>
  {{#each posts}}
  <div class="card">
    <div class="card-content">
      <a class="card-title" href="/post/{{id}}">
        {{ title }} -
        {{ author.name }} -
        {{mediatype}} -
        {{ likes }} Likes
      </a>
      <a href="/post/{{id}}"><img src="https://s3.amazonaws.com/stagebright/uploads{{url}}" alt="" class="img-responsive"></a>
    </div>
    <div class="card-action">
      <a href="/user/{{ author.id }}" class="">{{ author.email }}</a>
      <span class="">{{ createdAt }}</span>
    </div>
  </div>
  {{/each}}
</ul>
{{/if}} --}}

<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    {{#each posts}}
    <div class="carousel-item">
      <img class="d-block w-100" src="https://s3.amazonaws.com/stagebright/uploads{{url}}" alt="First slide">
    </div>
    {{/each}}
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>